/**
 * Shared primitives every uxmlib module builds on: version metadata, small null-safe helpers, and the
 * cross-cutting abstractions (scheduler, text, config) that land here as the library grows.
 */
@NullMarked
package com.uxplima.uxmlib.common;

import org.jspecify.annotations.NullMarked;
