/**
 * Soft-dependency hooks (PlaceholderAPI, Vault, LuckPerms): each binds only when its plugin is present,
 * so a server without them loads and runs the library unchanged.
 */
@NullMarked
package com.uxplima.uxmlib.hook;

import org.jspecify.annotations.NullMarked;
