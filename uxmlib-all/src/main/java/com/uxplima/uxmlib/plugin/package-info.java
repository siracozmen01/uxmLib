/**
 * The standalone-plugin shell. When uxmlib is installed as a single server-side dependency jar (rather
 * than shaded into a consumer), this thin {@code JavaPlugin} is the entry point that announces the
 * library is present; the toolkit itself is consumed as an API by other plugins.
 */
@NullMarked
package com.uxplima.uxmlib.plugin;

import org.jspecify.annotations.NullMarked;
